language: generic
sudo: required
services:
	- docker

before_install:
	- docker build -t nummunoil/docker-react -f Dockerfile.dev .

script:
	- docker run -e CI=true nummunoil/docker-react npm run test
